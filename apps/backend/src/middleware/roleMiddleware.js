/**
 * FILE: src/middleware/roleMiddleware.js
 * -----------------------------------------------------------------------------
 * PURPOSE: Enforces Role-Based Access Control (Authorization).
 * -----------------------------------------------------------------------------
 * RESPONSIBILITIES:
 * 1. Check if the authenticated user has the required Role (e.g., Admin).
 * 2. Allow access if roles match; deny access (403 Forbidden) if they don't.
 */
