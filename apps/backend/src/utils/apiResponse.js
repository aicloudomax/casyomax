/**
 * FILE: src/utils/apiResponse.js
 * -----------------------------------------------------------------------------
 * PURPOSE: Standardize API Responses.
 * -----------------------------------------------------------------------------
 * RESPONSIBILITIES:
 * 1. Provide a helper function to send success responses (uniform JSON format).
 * 2. Provide a helper function to send error responses.
 * * GOAL: Ensure the frontend always receives the same data structure.
 */
